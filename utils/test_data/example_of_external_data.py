# Basically just a proof that I understand how to import/export variables
# Normally I would use similar approach for selectors too, but that would look weird in this project's case

url = "https://vuejs.org/examples/#form-bindings"
input_field_value_updated = "hello123"
multi_check_list = [ "Jack", "John", "Mike" ]
input_radio_selectors = ["one", "two"]
input_radio_vals = ["One", "Two"]
select_vals = ["Please select one", "A", "B", "C"]
multi_select_selectors = [] # This is a bit silly I admit.
multi_select_vals = ["A", "B", "C"]